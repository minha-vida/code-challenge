
@{
    ViewData["Title"] = "Index";
}
<h2 class="title">Vacinas</h2>

<div class="wrapper">
    <div class="table-responsive">
        <table class="table" id="listagem-vacinas">
            <thead>
                <tr>
                    <th>Nome</th>
                    <th>CriadaEm</th>
                    <th>AlteradaEm</th>
                    <th class="col-xs-1">Ações</th>
                </tr>
            </thead>
            <tbody></tbody>
        </table>
    </div>
</div>

<a href="@Url.Action("Cadastrar","Vacina")">Cadastro</a>

@section Scripts {
    <script src="@Url.Content("~/js/Vacina/Vacina-ui.js")" type="text/javascript"></script>
    <script src="@Url.Content("~/js/Vacina/Vacina.js")" type="text/javascript"></script>

    <script type="text/x-jsrender" id="listagem-template">
        <tr>
            <td>{{:nome}}</td>
            <td>{{:CriadaEm}}</td>
            <td>{{:AlteradaEm}}</td>
            <td>
                <div class="btn-group">
                    <button type="button" class="btn btn-primary dropdown-toggle" data-toggle="dropdown">
                        Ações <span class="caret"></span>
                    </button>
                    <ul class="dropdown-menu pull-right" role="menu">
                        <li><a href="@(Url.Action("Cadastrar", "Vacina"))/{{:id}}">Editar</a></li>
                        <li><a href="#" id="Excluir" data-id="{{:id}}">Excluir</a></li>
                    </ul>
                </div>
            </td>
        </tr>
    </script>

    <script type="text/x-jsrender" id="sem-resultado-template">
        <tr>
            <td colspan="100%">
                <div class="alert alert-danger">Nenhum resultado encontrado</div>
            </td>
        </tr>
    </script>
}

